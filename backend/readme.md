TUTORIAL AKSES BACKEND

1. python -m venv venv

2. venv\Scripts\activate

3. PIP INSTALL dengan cara pip install -r requirements.txt

4. SETTING .env yang sesuai

5. uvicorn app.main:app --reload


BUKA DOKUKENTASI API DI http://localhost:8000/docs#/